console.log('A variable declared by let, in a function still exists after the function finishes running');
console.log("A variable declared by var, in a function don't still exists after the function finishes running");